services:
  - type: web
    name: mysmartroom-property-ai
    env: node
    buildCommand: npm install && npm rebuild better-sqlite3 --build-from-source && npm run build
    startCommand: npm start
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: NODE_ENV
        value: production
